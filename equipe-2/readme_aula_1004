""" melhorar a quebra de linha 
    inserir mais informações sobre o hardware utilizado, assim como o sistema operacional
    desenvolver sobre a metodologia utilizada para que chegasse nos resultados da avaliação experimental
    inserir mais informações acerca do item 3, sobre o ambiente utilizado """

#ITEM 1 - Quicksort paralelo
Os algoritmos usados nesse  trabalho foram Ordenação  por  Amostragem  e
Quicksort  Paralelo.  Ambos são métodos de ordenação  baseado  na 
divisão  do  arquivo  de entrada em subconjuntos, a ordenação por amostragem algoritmo
tem como etapa  chave a divisão  das  partiçõesde maneira balanceada, para que cada processador recebaaproximadamente  a  mesma  carga  de  dados e o quicksort paralelizado utiliza pivôs para realizar o
particionamento recursivo dos dados no conjunto de processadores
(nesse trabalho o pivô foidefinido como a mediana de n valores do conjunto de dados).

Na versão paralela, em vez de dividir e ordenar recursivamente todas as partições
sequencialmente, várias partições
são processadas em paralelo por diferentes threads ou processos. Isso permite que o
algoritmo aproveite melhor os recursos de hardware modernos, como CPUs com vários
núcleos ou clusters de computadores.

O Quicksort paralelo geralmente requer sincronização entre as threads para garantir
que as partições sejam corretamente combinadas e ordenadas. No entanto, quando implementado
corretamente, o Quicksort paralelo pode oferecer melhor desempenho em comparação com a versão
sequencial, especialmente para conjuntos de dados grandes.

#ITEM 2 Sobre o map reduce

MapReduce é um modelo de programação e um sistema de processamento de dados
distribuído usado para lidar com grandes volumes de dados de forma eficiente.
Ele divide tarefas em duas etapas principais: o mapeamento (map) e a redução (reduce).

No estágio de mapeamento, os dados são divididos em partes menores e processados em paralelo
em várias máquinas. Cada máquina aplica uma função de mapeamento aos dados e gera uma lista de
pares chave-valor intermediários.

Em seguida, no estágio de redução, os dados intermediários são agrupados com base em suas chaves
e processados para produzir resultados finais. Cada grupo de valores associados à mesma chave é
passado para uma função de redução, que combina esses valores em um único resultado.

O MapReduce permite distribuir e paralelizar o processamento de grandes conjuntos de dados em um cluster
de computadores, oferecendo escalabilidade e tolerância a falhas. É amplamente utilizado em sistemas de
análise de dados, processamento de logs, indexação da web e em muitos outros cenários onde lidar com grandes
volumes de dados é essencial.

#ITEM 3
A linguagem é Java.

#ITEM 4

O artigo define que o algoritmo do quicksort paralelo em relação ao de ordenação por amostragem,
no quesito de estabilidade, apresenta um tempo quase similar de execução que o outro em um mesmo
tipo de teste. Além disso, as partições formadas são balanceadas para ambos os algoritmos com um número de elementos iguais, assim, os tempos de ordenação são similares. Além disso, a distribuição dos valores não influencia na ordenação e formação de partições, porém, um jeito de modificar o tempo de ordenação é aumentando o conjunto de dados de forma a melhorar o tempo de ordenaça como resultado. A eficiência do algoritmo de
ordenação por amostragem apresenta uma eficiência de 93% sobre o quicksort paralelo de acordo com os valores de speedup e eficiência subliminares.
